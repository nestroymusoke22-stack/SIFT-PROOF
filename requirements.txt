# SIFT-PROOF runtime dependencies
#
# Core forensic parsing libraries (used by the native, cross-platform parsers
# under tools/ when an investigation runs against a mounted volume or memory dump):
python-registry>=1.3.1     # native registry Run-key / Amcache.hve parsing
python-evtx>=0.8.1         # native Windows Event Log (.evtx) parsing
volatility3>=2.28.0        # memory-image analysis (pslist/netscan/cmdline/malfind)

# Default LLM backend (Groq free tier — set GROQ_API_KEY to use it):
groq>=1.4.0

# Optional LLM backends (uncomment the one you intend to use):
#   AI_BACKEND=ollama      -> pip install requests   (local, offline)
#   AI_BACKEND=anthropic   -> pip install anthropic   (paid)
# requests>=2.31.0
# anthropic>=0.39.0
